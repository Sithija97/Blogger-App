export enum USER_ROLES {
  USER = "user",
  MODERATOR = "moderator",
  ADMIN = "admin",
}

export enum LoadingStates {
  IDLE = "idle",
  LOADING = "loading",
  SUCCESS = "success",
  FAILURE = "failure",
}
